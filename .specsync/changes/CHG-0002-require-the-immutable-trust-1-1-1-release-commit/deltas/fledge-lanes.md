## MODIFIED

### REQUIREMENT REQ-fledge-lanes-011

The repository SHALL run the unified Trust gate on pull requests and main-branch pushes using the immutable Trust 1.1.1 release commit.

Acceptance Criteria
- Deterministic validation confirms the pull-request and main-branch push triggers and the exact immutable Trust action pin.
