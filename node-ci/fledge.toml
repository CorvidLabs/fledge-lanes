[tasks.install]
cmd = "npm ci"

[tasks.lint]
cmd = "npm run lint"
deps = ["install"]

[tasks.typecheck]
cmd = "npm run typecheck"
deps = ["install"]

[tasks.test]
cmd = "npm test"
deps = ["install"]

[tasks.build]
cmd = "npm run build"
deps = ["typecheck"]
env = { NODE_ENV = "production" }

[lanes.ci]
steps = ["install", "lint", "typecheck", "test", "build"]
fail_fast = true

[lanes.check]
steps = ["install", { parallel = ["lint", "typecheck"] }, "test"]
fail_fast = true

[lanes.build-only]
steps = ["install", "typecheck", "build"]
